const form = document.getElementById("formTaskSeventeen");

form.addEventListener("submit", function (event) {
  event.preventDefault();
  console.log("The button is pressed, but the page does not reload");
});
